# FRESCO's full text log is regenerable from the .nin input via run_fresco.sh;
# only the parsed per-state cross sections are tracked, since the macros read those.
outputs/*.out

# ad-hoc scan artefacts and the fitter scratch dir
inputs/sens_*.nin
outputs/sens_*
outputs/_fit/
__pycache__/
